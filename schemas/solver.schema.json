{
  "type": "object",
  "additionalProperties": false,
  "required": ["task_id", "title", "approach", "answer_fragment", "confidence", "assumptions", "memory_updates"],
  "properties": {
    "task_id": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "approach": {
      "type": "string"
    },
    "answer_fragment": {
      "type": "string",
      "description": "The proof body in LaTeX (e.g. \\begin{proof}...\\end{proof} or an amsthm-style proof environment)."
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "assumptions": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "memory_updates": {
      "type": ["array", "null"],
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["type", "fact_id", "fact_kind", "statement", "justification", "implication_id", "premise_fact_ids", "conclusion_fact_id", "reason"],
        "properties": {
          "type": {"type": "string", "enum": ["fact_upsert", "fact_delete", "implication_upsert", "implication_delete"]},
          "fact_id": {"type": ["string", "null"]},
          "fact_kind": {"type": ["string", "null"], "enum": ["hypothesis", "definition", "derived", "external", null]},
          "statement": {"type": ["string", "null"]},
          "justification": {"type": ["string", "null"]},
          "implication_id": {"type": ["string", "null"]},
          "premise_fact_ids": {"type": ["array", "null"], "items": {"type": "string"}},
          "conclusion_fact_id": {"type": ["string", "null"]},
          "reason": {"type": ["string", "null"]}
        }
      }
    }
  }
}
